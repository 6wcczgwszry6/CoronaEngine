=== F2 [致命] set_visibility_config 静默清除距离配置 ===
文件: src/systems/script/python/corona_engine_api.cpp
引入: commit f7a5b37c

问题: set_visibility_config 创建全新的 SceneVisibilityConfig 替换整个 cfg，
     未填字段归零，导致之前 set_distance_config 的设置被静默清除。

修复: 改为先读取现有配置，只修改可见性字段，保留距离字段不变。
      需要 GeometrySystem 暴露一个 getter 或 partial-setter。

方案 A (最小改动): 在 GeometrySystem 加 set_invisible_frames_to_evict 方法，
                 只修改单个字段不覆盖其他配置。

--- a/include/corona/systems/geometry/geometry_system.h
+++ b/include/corona/systems/geometry/geometry_system.h
@@ -163,6 +163,9 @@ public:
     /// 距离卸载配置接口
     void set_distance_config(std::uintptr_t scene, float unload_dist, float preload_dist, bool enable = true);

+    /// 设置连续不可见帧驱逐阈值（只修改此字段，不覆盖距离配置）
+    void set_invisible_frames_to_evict(std::uintptr_t scene, int frames);
+
     // ========================================
     // 空间查询（线程安全）
     // ========================================

--- a/src/systems/geometry/geometry_system.cpp
+++ b/src/systems/geometry/geometry_system.cpp
@@ -564,6 +564,14 @@ void GeometrySystem::set_distance_config(std::uintptr_t scene, float unload_dist
     scene_state.cfg.preload_distance = preload_dist;
 }

+void GeometrySystem::set_invisible_frames_to_evict(std::uintptr_t scene, int frames) {
+    std::unique_lock lock(impl_->mtx);
+    auto& scene_state = impl_->get_or_create(scene);
+    scene_state.cfg.invisible_frames_to_evict = frames;
+    // collect_stats 和其他距离字段保持不变
+}
+
 void GeometrySystem::set_cache_directory(std::filesystem::path dir) {
     ...

--- a/src/systems/script/python/corona_engine_api.cpp
+++ b/src/systems/script/python/corona_engine_api.cpp
@@ -559,9 +559,7 @@ void Corona::API::Scene::set_visibility_config(int invisible_frames_to_evict) {
         CFW_LOG_ERROR("[Scene::set_visibility_config] GeometrySystem not found");
         return;
     }
-    Corona::Systems::SceneVisibilityConfig cfg;
-    cfg.invisible_frames_to_evict = invisible_frames_to_evict;
-    cfg.collect_stats = true;
-    geom_sys->set_visibility_config(handle_, cfg);
+    // 使用 partial setter，只修改可见性字段，不覆盖距离配置
+    geom_sys->set_invisible_frames_to_evict(handle_, invisible_frames_to_evict);
 }
