# R3 黑盒期单人垂直切片推进计划

更新时间：2026-07-20
状态：黑盒成果转实机收敛权威子计划
适用范围：保留黑盒期已验证的三职能骨架，将真实 LANChat 入口、AgentRuntime、SceneWorldSnapshot 与单人 Demo Gate 收敛为一条生产主链

> 本文档是 `R3稳定门禁与三职能Agent双轨推进计划.md` 的限时执行子计划，不替代完整 R3 目标。
> 当两份文档发生冲突时，RuntimeGuard、EngineWriteGate、真实 Engine 事实、Mock 不可执行和完整 R3 诚实披露等架构不变量以总计划为准。
> 本文档只调整一周内的实施顺序、单人能力解锁和黑盒验证方式。
> 每轮执行同时遵守 `Agent任务约束循环_R3三职能协同版.md`，进度写入 `R3-min推进记录.md`。

---

## 1. 当前目标、约束与事实起点

### 1.1 本周目标

在低美术质量可接受的前提下，形成一条约 40-50 秒可玩的单人垂直链：

```text
用户提出 Demo 目标
-> 策划 Agent：GameDesignBrief + LevelPlan
-> 程序 Agent：GameplayLogicPlan
-> 美术 Agent：ArtDirection + SceneCompositionPlan
-> 房主确认
-> 真实文生图
-> 严格图生模型
-> AgentRuntime 场景生成
-> SceneWorldSnapshot
-> EntityBindingPlan
-> 白名单玩法原语
-> 单人预览与完成目标
```

固定玩法：

```text
玩家出生
-> 走到桌边拾取钥匙
-> 使用钥匙解锁门
-> 进入终点区域
-> 完成目标并显示成功
```

### 1.2 当前事实

- 完整 R3 Gate 仍为 `red / pending_reevaluation`。
- 最新实机证据为 `build/examples/engine/RelWithDebInfo/logs/2026-07-21_02-54-38_corona.log`。
- 本轮讨论使用 `deepseek/deepseek-v4-pro` 成功；方案链完成 Planning 与 Program，Art 调用后因 `scene_composition_plan.entity_requirements` 未覆盖程序权威 semantic roles 而以 `gameplay_roles_missing` fail closed。
- Narrator、Proposal、图片、模型、Actor 和 Runtime 写入均未进入；当前阻断已从程序契约收敛到美术角色契约。
- 黑盒阶段 B0-B4 已建立 Walking Skeleton、ArtifactRegistry/TaskGraph、GameplayLogicPlan 1.1、ProjectGatePreflight、Demo Runner、Engine Test Double 和 Adapter 契约。
- 当前代码已完成真实讨论、三职能 Coordinator、版本化方案身份、严格媒体血缘和 Finalizer 幂等收口；仍需新独立 Session F5 验证。
- Skeleton contract 已因 `Planning -> Program -> Art` 节点依赖和 Program 公共接口变化升为 `r3-skeleton-week1-v6`，hash 为 `sha256:6144cabd279c57c8e843c585156e775f213d2575f1c61152100a426f5729e1cd`。
- 3 项 `sys.modules` 隔离误报已经改为目标模块 AST import 检查，不再作为已知门禁噪声。
- 上一次 F5 仍走旧生产入口；当前代码已把方案请求接入 CollaborationCoordinator，但生产接入状态只能标记 `code_complete / waiting_independent_f5`。
- 本阶段不继续纵向扩充黑盒模块，改为真实讨论、三职能协作、严格媒体链、单人 Gate 和分阶段 F5。

### 1.3 本周状态口径

只允许使用以下完成口径：

```text
code_complete
  代码与聚焦自动测试完成，尚无集成或实机证据

integration_ready
  契约、Adapter、Test Double、Runner 和迁移清单完整，等待稳定底层 SHA

single_player_verified
  在稳定 Engine SHA 上通过最小 F5 和两次独立单人 Demo Session

full_r3_verified
  仍按完整 R3 文档执行，本周不默认宣称完成
```

`integration_ready` 不等于 Engine-ready、Game-ready 或完整 R3 Green。

### 1.4 黑盒成果实机映射

| 黑盒成果 | 当前状态 | 最新 F5 暴露的集成缺口 |
|---|---|---|
| B0 Walking Skeleton | verified | 未接入 LANChat 生产入口 |
| B1.1 Engine capability port | code_complete | 实机路径没有读取稳定 capability manifest |
| B1.2 Frontend Adapter | code_complete | Runtime 仍直接发送聊天消息，绕过 Adapter |
| B2.1 Engine Test Double | verified | 没覆盖真实消息、回复和 Finalizer 披露行为 |
| B2.2 双版本兼容 | pending | 不再阻断纯 Gate 开发，只阻断跨版本迁移 |
| B3 GameplayLogicPlan | verified | 尚未连接真实 SceneWorldSnapshot |
| B4 Preflight/Runner | verified | 尚未成为用户请求的实际处理链 |
| D1 方案权威与确认事务 | code_complete | 唯一 ID/version/hash 与 Frontend metadata 待 B7.1 |
| D2 真实三职能与 Narrator | code_complete | 四次真实模型调用与自然语言方案待 B7.1 |
| D3 图片资源解析与媒体血缘 | code_complete | MediaRegistry fileid、Hunyuan 和 Engine Actor 待 B7.2 |
| D4 失败终态幂等 | code_complete | 后续问候 internal graph 增量为 0 待 B7.1/B7.2 |
| D5 进度与用户披露 | code_complete | Frontend 稳定 event ID 原位更新待 B7.1 |

黑盒成果不回滚，也不冒充生产接入完成。后续每个任务同时记录 `contract_status` 与 `production_integration_status`。

---

## 2. 架构不变量与责任边界

### 2.1 现有 Runtime 主链保持不变

```text
ScenePlan -> BatchPlan -> ToolCallGraph
-> RuntimeGuard -> EngineWriteGate / RuntimeCppBridge
-> ToolResult -> StatePatch
-> RuntimeState -> OperationLog
-> Registry -> SceneWorldSnapshot
```

硬约束：

1. Agent、Coordinator 和 ProjectGate 不直接写 Engine。
2. 不新增可供上层直接调用的公共 `EngineAdapter.create_actor()`。
3. Engine 写入继续使用已有 `RuntimeCppBridge`，并且只能在 EngineWriteGate 后执行。
4. Frontend 不承载项目状态、Gate 结论或执行事实判断。
5. Mock / fixture 可以用于测试，但不能构造可执行 ActionProposal。
6. Engine 返回事实优先于 Agent、计划、名称和估算结果。
7. EntityBindingPlan 只能引用指定 Snapshot 版本中的稳定 `entity_id`。
8. 不恢复 SceneComposer / ProgressiveWorkflow 为用户主入口。

### 2.2 三方责任

```text
AI / Runtime 组
  领域契约、Python 端口、Test Double、Gate profile、Demo Runner、证据包

Engine 组
  Native method/event manifest、白名单玩法原语、稳定 24 小时集成 SHA

Frontend 组
  UserCommand / ProgressEvent 映射、确认与进度展示、预览入口

集成 owner
  锁定 SHA、维护兼容差异表、组织第 6-7 天 F5 和结果归档
```

Adapter 采用共同契约、分层维护，不由单一团队跨层修改全部实现。

### 2.3 框架先行：Walking Skeleton

本周不采用“完整写完一个模块，再开始下一个模块”的纵深顺序。先建立一条可运行、可阻断、可观测的端到端骨架，再沿骨架填充各层细节：

```text
UserCommand fixture
-> Demo Scenario Runner
-> Planning / Art / Program Agent
-> Artifact bundle
-> ProjectGatePreflight
-> EngineCapabilityPort
-> DemoResult(status=pending_runtime_verification)
-> ProgressEvent fixture
```

Walking Skeleton 必须满足：

- 数据结构、依赖方向、调用入口和错误传播真实存在。
- 每层只有最小确定性实现，不提前扩展完整业务功能。
- 未实现的 Engine / Frontend 能力返回 `unavailable`、`blocked` 或 `pending_runtime_verification`。
- 禁止 `pass`、空字典、固定“成功”或 Mock Green 作为临时实现。
- 骨架不创建 ActionProposal、PlanPatch、ToolCallGraph 或 Engine 写入。
- 骨架通过后，B1-B5 只能沿既有端口补细节，不再引入第二套主链。

框架先行不是大而全设计。每个骨架接口必须服务本周固定 Demo，并通过一项端到端契约测试；不为未来战斗、剧情、蓝图或多人能力预建空壳。

### 2.4 架构 AI 与填充 AI 串行交接

本周采用串行交接，不允许两个角色并发修改骨架：

```text
架构 AI
-> 完成 B0.1-B0.4
-> 冻结接口、节点 ID、数据流和 skeleton contract hash
-> 输出 SkeletonStatusReport

填充 AI
-> 从优先级最高且依赖满足的 blocked 节点开始
-> 每轮只填充一个节点
-> 不修改已冻结公共接口
```

架构 AI 不只创建接口签名，还必须完成最小编排、统一阻断诊断和端到端骨架测试。填充 AI 如发现接口不足，必须提交 `InterfaceChangeRequest` 并停止，不得自行改变签名或建立旁路；只有架构 AI 可以作出 `InterfaceChangeDecision`。

### 2.5 黑盒骨架与生产入口收敛

当前两条链路必须收敛，禁止继续独立演进：

```text
当前实机链：
LANChat -> 旧 RoleAgent / lanchat_scene_runtime -> SeedPlan -> AgentRuntime

目标生产链：
UserCommand / 兼容聊天入口
-> MessageDispatchLedger
-> Collaboration Runner
-> 强类型 Artifact / ProjectGate
-> 现有 AgentRuntime
-> SceneWorldSnapshot
-> Frontend ProgressEvent
```

旧“小女孩/长者/商人”等 persona 只保留兼容入口，不再继续扩建其规划、执行或项目状态能力。兼容入口只能把用户目标转换为统一 UserCommand 或提供普通聊天回复，不得建立第二套 ScenePlan、实体绑定或 Engine 写入协议。

在 Full R3 Red 期间，Collaboration Runner 只允许产出 Artifact、Preflight 和阻断诊断；只有 Single-player Demo Gate Green 后才能构造可执行 ActionProposal。

---

## 3. 隔离架构与公共契约

### 3.1 Engine 隔离层

生产实现继续复用 `services/agent_runtime/adapters.py`。新增的稳定端口仅覆盖能力发现和只读事实：

```python
class EngineCapabilityPort(Protocol):
    def get_manifest(self) -> EngineCapabilityManifest: ...

class EngineSnapshotPort(Protocol):
    def get_snapshot(self, request: SceneSnapshotRequest) -> SceneWorldSnapshot: ...
```

写入边界继续使用：

```python
RuntimeCppBridge.invoke_tool(...)
```

不得让 `services/agent_collaboration/` import Engine port、RuntimeCppBridge、C++ DTO 或 SceneTools。

`EngineCapabilityManifest`：

```text
contract_version
bridge_version
snapshot_schema_version
supported_operations
supported_gameplay_primitives
```

单人 Demo 所需操作：

```text
actor_create
scene_snapshot
actual_aabb
render_ready
gameplay.apply_manifest
gameplay.preview.start
```

单人 Demo 所需玩法原语：

```text
on_enter
on_collect
set_state
unlock
complete_objective
```

若任一必要能力缺失，ProjectGate 必须返回 `engine_capability_missing`，不得回退任意脚本。

### 3.2 Frontend 隔离层

复用 `editor/Frontend/src/utils/bridge.js` 的 C++ manifest 通讯能力，只增加业务命令和事件映射层。

`UserCommand`：

```text
schema_version
command_id
room_id
command_type:
  start_project | confirm_action | query_status | start_preview
payload
```

`ProgressEvent`：

```text
schema_version
event_id
command_id
room_id
project_id
task_id
plan_id
scene_version
event_type
status
detail
```

约束：

- 相同 `command_id` 只能产生一次权威业务动作。
- 相同 `event_id` 在 Frontend store 中只能消费一次。
- AI 页面不得直接调用 `sceneTools.createActor`、`removeActor` 或其他场景写 API。
- Frontend 只展示结构化进度，不根据聊天文本推断完成状态。

### 3.3 分域版本

Python 侧版本统一放在 `editor/plugins/AITool/services/schema_versions.py`，禁止各模块重复声明：

```text
COLLABORATION_SCHEMA_VERSION = "1.2"
SCENE_WORLD_SNAPSHOT_SCHEMA_VERSION = "r3-scene-world-v1"
R3_GATE_SCHEMA_VERSION = "r3-gate-v1"
ENGINE_ADAPTER_CONTRACT_VERSION = "r3-engine-week1-v1"
FRONTEND_INTERACTION_SCHEMA_VERSION = "r3-interaction-week1-v1"
SKELETON_CONTRACT_VERSION = "r3-skeleton-week1-v1"
```

版本规则：

- Runtime、协作层和 Python Adapter 只能从 `schema_versions.py` import 版本。
- Engine / Frontend 的自身版本由各自 manifest 返回，Python Adapter 与集中登记版本做兼容检查。
- 第 2 天后发生破坏性变更必须提升对应 domain version。
- 历史 Snapshot / Artifact 可读取和审计，但标为 legacy 或 stale。
- legacy Snapshot 不得进入 ProjectGate 的可执行路径。
- schema version 必须参与 Artifact content hash、Snapshot fingerprint 或 Gate evidence。

### 3.4 统一阻断诊断

所有骨架节点使用同一个无 Runtime、Frontend、C++ 依赖的结果契约：

```python
@dataclass(frozen=True)
class MissingRequirement:
    requirement_id: str
    owner_domain: str
    description: str

@dataclass(frozen=True)
class BlockedResult:
    node_id: str
    status: str  # unavailable | blocked | pending_runtime_verification
    error_code: str
    summary: str
    missing_requirements: tuple[MissingRequirement, ...]
    owner_domain: str
    retryable: bool
    next_action: str
    evidence_refs: tuple[str, ...]
```

`owner_domain` 只允许：

```text
ai_runtime | engine | frontend | collaboration | integration
```

约束：

- `error_code`、`owner_domain` 和 `next_action` 必填。
- `requirement_id` 必须匹配 `^[a-z][a-z0-9_.-]{2,63}$`，例如 `engine.gameplay_apply_manifest`；禁止使用自然语言句子作为 ID。
- `MissingRequirement.owner_domain` 必须属于允许的责任域。
- `unavailable/blocked/pending_runtime_verification` 均至少包含一个 MissingRequirement。
- 诊断不得包含密钥、签名 URL、原始 Provider 响应或异常堆栈。
- 未实现节点不得返回 `{}`、`True`、`None` 或固定 success。

骨架同时输出：

```text
SkeletonNodeStatus:
  node_id / interface_name / status
  blocker_code / owner_domain / fill_priority / evidence_refs

SkeletonStatusReport:
  contract_version / contract_hash
  nodes / overall_status / generated_at
```

### 3.5 Skeleton contract hash

contract hash 不对 `SkeletonStatusReport`、原始源码文件字节或自然语言说明做 hash。架构 AI 必须先生成规范化 `SkeletonContractManifest`：

```text
schema_versions
public_protocols:
  protocol name
  ordered method signatures
  parameter names/types/defaults
  return types
public_dtos:
  DTO name
  ordered field names/types/defaults
public_enums:
  enum name / ordered values
skeleton_nodes:
  node ID / interface name / ordered edges
```

Manifest 使用 canonical JSON 序列化，再计算：

```text
sha256(canonical_json(SkeletonContractManifest))
```

规则：

- 接口签名、DTO 字段、枚举、schema version、节点 ID 或边顺序变化必须改变 hash。
- 注释、空白、格式化和私有实现变化不得改变 hash。
- SkeletonStatusReport 只引用 contract hash，不参与 hash 计算。
- 填充 AI 开始任务前必须校验当前 hash 与交接 hash 一致。

### 3.6 接口变更交接契约

填充 AI 发现冻结接口不足时提交：

```text
InterfaceChangeRequest:
  request_id
  node_id
  detected_by_task_id
  current_contract_version
  current_contract_hash
  reason_code
  required_change
  affected_interfaces
  blocked_dependents
  evidence_refs
```

处理顺序固定为：

```text
填充 AI 停止节点实现
-> 节点状态改为 blocked/interface_change_required
-> 写入 InterfaceChangeRequest
-> 架构 AI 判断是否可由私有实现解决
-> 输出 InterfaceChangeDecision
```

`InterfaceChangeDecision`：

```text
decision: accepted | rejected | no_contract_change
reason
changed_interfaces
new_contract_version
new_contract_hash
affected_nodes
required_revalidation
evidence_refs
```

若接受公共接口变更：

- 提升 `SKELETON_CONTRACT_VERSION`。
- 更新 Manifest 并重新计算 hash。
- 重跑 B0.4 全部骨架和架构依赖测试。
- 使用旧 hash 的下游节点标记 `stale/revalidation_required`。
- 形成新的架构 AI 交接包后，填充 AI 才能恢复。

---

## 4. GameplayLogicPlan 1.1

### 4.1 目标

保留六种首批 Artifact，不新增 `DemoGameplaySpec`。将白名单玩法原语加入现有 `GameplayLogicPlan`。

### 4.2 新增契约

```python
@dataclass(frozen=True)
class GameplayEntitySlot:
    slot_id: str
    semantic_role: str
    required_capabilities: tuple[str, ...]

@dataclass(frozen=True)
class GameplayPrimitiveSpec:
    primitive_id: str
    kind: str
    subject_slot: str
    target_slot: str
    parameters: Mapping[str, Any]
```

`GameplayLogicPlan` 1.1 至少包含：

```text
states
entity_slots
primitives
win_conditions
lose_conditions
```

### 4.3 Validator 规则

- `slot_id` 和 `primitive_id` 在单个 Artifact 内唯一。
- primitive 引用的 subject/target slot 必须存在。
- `kind` 必须属于白名单。
- `on_enter` 必须具有触发区域 subject slot。
- `on_collect` 必须具有可收集实体 subject slot。
- `set_state` 必须声明 state key 与目标值。
- `unlock` 必须具有 target slot 和解锁前置状态。
- `complete_objective` 必须声明 objective ID。
- 未知参数、空 ID、循环引用或不存在的 slot 使 Artifact invalid。
- 1.0 Artifact 保留审计，但不得作为新的可执行输入。

### 4.4 绑定规则

黑盒期只生成逻辑 slot，不产生真实 EntityBindingPlan。

真实 Snapshot 可用后：

```text
GameplayEntitySlot
-> ProjectGate 校验 Snapshot 候选
-> ProgramAgent 生成 EntityBindingPlan
-> slot_id -> stable entity_id
```

禁止通过显示名称、模型路径、数组顺序或聊天文本猜测绑定。

---

## 5. 黑盒验证模型

### 5.1 Engine Test Double

Test Double 只放在测试边界，模拟：

```text
create accepted
-> actor handle
-> geometry ready
-> actual AABB
-> grounding/support known
-> render ready 或 not_applicable
-> Snapshot version +1
```

必须提供六类确定性 fixture：

```text
normal
late_ready
partial
failed
duplicate_request
version_conflict
```

Test Double 规则：

- 所有 Snapshot 强制 `snapshot_source=mock`。
- actor/entity/asset ID 必须稳定且可重复计算。
- 不访问 C++、CEF、网络或模型 Provider。
- 不修改生产 RuntimeState 文件。
- `assert_executable()` 必须阻断其产生的 Artifact。

### 5.2 ProjectGatePreflight

黑盒期只执行 Preflight：

```text
Artifact schema
content hash
dependency/version
stale status
required Engine capabilities
```

Runtime Gate 不可用时必须返回：

```text
status=pending_runtime_verification
executable=false
```

禁止使用“跳过 Runtime Gate”“假定 Engine ready”或手工设置通过状态。

### 5.3 Demo Scenario Runner

Runner 不依赖 UI、不写 Engine，调用正式 ProjectState、ArtifactRegistry、AgentTaskGraph 和三个职能 Agent。

固定流程：

```text
ProjectGoal
-> PlanningAgent
-> ArtAgent
-> ProgramAgent
-> Artifact bundle
-> ProjectGatePreflight
-> DemoResult
```

`DemoResult`：

```text
status: integration_ready | blocked | failed
project_id
task_graph_id
artifact_refs
required_capabilities
preflight_result
pending_runtime_verifications
```

Runner 不构造 ActionProposal，不创建 PlanPatch，不创建 ToolCallGraph。

---

## 6. 单人 Demo Gate Profile

### 6.1 与完整 R3 的关系

`runtime.r3_readiness.evaluate` 支持：

```text
profile=full_r3
profile=single_player_demo
```

默认继续使用 `full_r3`。单人 profile 不覆盖或修改完整 R3 的结果。

### 6.2 动态要求来源

Runtime 层不得读取 GameplayLogicPlan。

ProjectGate 从已经验证的 `GameplayEntitySlot` 派生：

```text
DemoReadinessRequirement:
  requirement_id
  semantic_role
  required_capabilities
  min_count
```

Runtime evaluator 只检查这些结构化要求，不理解 Artifact 语义。GateReport 必须记录 requirements fingerprint 和匹配到的 entity IDs。

不得在 Gate 代码中硬编码 `player_spawn`、`collectible_key`、`locked_door` 或 `goal_zone`。

### 6.3 单人 Green 条件

必须全部满足：

- 必要环境实体 ready。
- Snapshot immutable 且 fingerprint 一致。
- Registry、Consistency Audit、Snapshot、Report 终态完整。
- 业务 Batch 与 business ToolGraph 数量和终态一致。
- RuntimeGuard 与 EngineWriteGate 边界有效。
- 每个动态 DemoReadinessRequirement 都有足量 Game-ready entity 匹配。
- 匹配实体满足 slot 声明的 capability。
- `project_mode=single_player` 且 peer count 为 0。
- 不存在 entity ID、asset ID 或 scene version 矛盾。

单人 profile 只解锁：

```text
single_player_entity_binding
single_player_local_action
single_player_preview
```

不解锁 multiplayer capability。

### 6.4 GameplayManifest

EntityBindingPlan 通过后形成：

```text
schema_version
project_id
plan_id
scene_version
entity_bindings
primitives
objective_id
```

唯一写路径：

```text
ProjectGate
-> ActionProposal(execution_scope=single_player_local)
-> PlanPatch
-> business ToolCallGraph
-> RuntimeGuard
-> EngineWriteGate
-> gameplay.apply_manifest
```

ActionProposal 构造时必须调用 `assert_executable()`，并验证 Gate profile、Snapshot version 和 execution scope。

---

## 7. 工作块与依赖

```text
B0 基线与契约冻结
-> B0.4 端到端 Walking Skeleton
-> B1 Engine / Frontend 隔离端口细化
-> B2 Engine Test Double 与兼容 fixture

B0.4
-> B3 GameplayLogicPlan 1.1
-> B4 Demo Runner 与 Preflight

B0-B4 contract verified
-> B5 F5 反馈收口

并行支线：
B2.2a 当前实机严格 fixture
-> B6.3 single_player_demo Gate evaluator

B5 完成 + B6.1/B6.2/B6.3 integration_ready
-> B7.1 控制面 F5
-> B7.2 Scene Runtime 最小 F5
-> B6.4 执行边界
-> B7.3 完整单人 Demo
```

黑盒阶段 B0-B4 已结束。当前只允许 B5 主线与不消费真实 Engine 结果的 B2.2a/B6.3 纯契约支线并行；修改 `lanchat_agent_worker.py` 的控制面任务必须串行，避免多个 AI 同时改动同一入口。

---

## 8. B0：基线与契约冻结

### B0.1 基准 SHA 与改动清单

- 目标：建立黑盒开发期间的比较基准和无已知噪声的测试门禁。
- 输入：当前 AI 分支 SHA、Engine main SHA、Frontend main SHA、工作区状态。
- 实施：记录三方 SHA 和 API manifest；修复 3 项依赖全局 `sys.modules` 的隔离测试误报；黑盒期每日只生成差异报告，不自动合并 main。
- 交付物：基准清单、Adapter 影响表、集成 owner、干净的 Track B + R3 合并门禁。
- 完成：能够指出任一底层变化只影响哪个 Adapter 契约；81 项 Track B 与 21 项 R3 测试在同一执行中通过。
- 不做：清理用户未提交改动或频繁 rebase。

### B0.2 集中 schema version 登记

- 前置：B0.1。
- 目标：冻结六个 domain version，消除散落版本字符串。
- 实施：新增纯常量 `schema_versions.py`；Runtime、协作层和 Python Adapter 改为统一 import。
- 完成：序列化结果携带正确版本；legacy 输入可识别但不可执行；源码检查不存在重复版本声明。
- 不做：要求 C++ 或 JavaScript import Python 文件；外部版本通过 manifest 对账。

### B0.3 BlockedResult 与节点清单契约

- 前置：B0.2。
- 目标：让所有未实现节点可诊断、可归责、可排序。
- 实施：定义 MissingRequirement、BlockedResult、SkeletonNodeStatus、SkeletonStatusReport、SkeletonContractManifest、InterfaceChangeRequest/Decision 和 Validator。
- 完成：缺失诊断字段无法构造；相同节点事实产生相同状态表和 contract hash。
- 不做：把底层异常原文或敏感 Provider 信息写入诊断。

### B0.4 端到端 Walking Skeleton

- 前置：B0.1-B0.3。
- 目标：在深入实现任一模块前，固定本周端到端数据流和依赖方向。
- 实施：连接 UserCommand fixture、现有三个 Agent 的最小调用、Artifact bundle、ProjectGatePreflight、Engine capability unavailable 结果、DemoResult 和 ProgressEvent fixture。
- 交付物：一条无 UI、无 Engine 写入、默认 fail closed 的可运行骨架；SkeletonStatusReport；规范化 SkeletonContractManifest；冻结后的接口与 contract hash。
- 完成：固定 ProjectGoal 可得到五 Artifact bundle，Preflight 返回带完整 BlockedResult 的 `pending_runtime_verification`，DemoResult 为非执行状态，ProgressEvent 能表达阻断原因、责任域和下一动作。
- 验证：一项端到端 skeleton 测试；一项架构依赖测试；每个节点的 blocker code、owner 和 missing requirement 均可断言。
- 不做：真实 Engine Adapter、完整 Test Double、ActionProposal 成功路径、玩法执行和 UI 页面。

---

## 9. B1：Adapter 隔离端口

### B1.1 Engine capability / snapshot 端口

- 前置：B0.4。
- 实施：在 Runtime 内定义 Protocol 和 DTO；现有 adapters 实现端口。
- 完成：业务层不依赖 Native 字段名；Engine 写路径仍由 RuntimeCppBridge 控制。
- 验证：old/new Engine fixture 归一化测试、缺失 capability 测试。

### B1.2 Frontend 业务协议

- 前置：B0.4。
- 实施：定义 UserCommand/ProgressEvent；在现有 Bridge 上增加映射。
- 完成：UI 不直接写场景；相同 command/event 幂等。
- 验证：重复消息、乱序进度、未知版本、错误 payload 测试。

### B1.3 第 2 天联合冻结

- 参与：AI、Engine、Frontend owner。
- 冻结：operation 名称、字段、错误码、请求/响应样例和 manifest fixture。
- 规则：第 2 天后的破坏性变更必须升版本并更新兼容 fixture。

---

## 10. B2：Engine Test Double

### B2.1 生命周期模拟

- 前置：B1.1。
- 实施：实现六类 fixture 和可控生命周期推进。
- 完成：相同输入产生相同 ID、状态序列和 Snapshot fingerprint。
- 验证：normal、late-ready、partial、failed、duplicate、conflict。

### B2.2 Adapter 版本兼容拆分

#### B2.2a current-unversioned-v1 严格 fixture

- 前置：B2.1、本轮真实 F5 Snapshot 字段证据。
- 实施：冻结当前未携带显式 DTO version 的输入字段集合、build fingerprint 和必填身份字段，登记为 Adapter 内部 `current-unversioned-v1` fixture。
- 完成：只有字段集合和 fingerprint 精确匹配时才归一化到 public Snapshot；未知字段变化、缺失 actual 事实或不匹配 build 均 fail closed。
- 解锁：当前单人 Gate evaluator 和当前 SHA 的只读 Adapter 对账。
- 不做：通过 `actor_id/actor_guid`、`aabb/world_aabb` 等字段是否存在来宽松猜测版本。

#### B2.2b Engine 显式 input_dto_version

- 前置：Engine capability manifest 提供稳定输入 DTO 版本。
- 实施：将 Engine 显式版本传入 Adapter，并与 public SceneWorldSnapshot schema version 分离。
- 完成：当前与候选 Engine fixture 均能按显式版本归一化；不支持的版本返回 capability mismatch。
- 阻断范围：只阻断跨 SHA Adapter 迁移和 B7 最终集成，不再阻断 B5 或 B6.3 的纯逻辑开发。

---

## 11. B3：GameplayLogicPlan 1.1

### B3.1 强类型 slot 与 primitive

- 前置：B0.4。
- 实施：新增 GameplayEntitySlot、GameplayPrimitiveSpec，升级 ProgramAgent 输出。
- 完成：固定 Demo 可表达完整拾取、解锁、到达终点流程。
- 验证：合法链、未知 primitive、缺失 slot、重复 ID、非法参数测试。

### B3.2 版本与 stale 传播

- 前置：B3.1。
- 实施：1.0 GameplayLogicPlan 进入历史审计；1.1 变更触发 EntityBindingPlan 和下游输出 stale。
- 完成：旧 Artifact 不会静默操作新 Snapshot。

---

## 12. B4：Preflight 与 Demo Runner

### B4.1 ProjectGatePreflight

- 前置：B1.1、B3.1。
- 实施：校验 Artifact、能力需求和版本；Runtime Gate 缺失时 fail closed。
- 完成：只返回 integration readiness，不创建执行对象。

### B4.2 无 UI Demo Runner

- 前置：B4.1、现有五 Artifact workflow。
- 实施：用固定 ProjectGoal 驱动三 Agent 和 TaskGraph。
- 完成：生成确定性五 Artifact bundle 和 DemoResult。
- 验证：正常、Agent 失败重试、Artifact stale、capability 缺失、重复 command ID。

---

## 13. B5：F5 反馈收口

### B5.0 F5 证据基线

- 前置：B0-B4 已验证、本轮 F5 日志与 LANChat history 可读。
- 实施：把本轮日志和 history 作为只读回归输入，建立消息时间线与 F5 对账摘要。
- 输出：每条消息的 route、target agent、模型调用 purpose、final reply 数量、progress/action status 数量、绑定 plan/artifact 和终态事件顺序。
- 完成：能够自动复现本轮的错误角色回复、双 final reply、语法片段实体、20 条重复 heartbeat 和 report/snapshot 披露乱序。
- 不做：修改 Runtime、Frontend 或 Engine 行为。

### B5.1 单消息单执行单回复

- 前置：B5.0。
- 实施：Native Queue 与 Agent Trigger 共用 `MessageDispatchLedger`；为 route、execution owner、reply owner 和 final reply 增加原子状态转换。
- 完成：显式目标 Agent 不得被 pending Agent 替换；每条消息最多一条 final business reply；消息重放不产生新模型调用或回复。
- 规则：`progress`、`action_status` 和内部审计不是 final reply，但必须携带原始 message/correlation identity。
- 验证：Native-first、Trigger-first、并发、重放、发送失败重试和错误目标回退。

### B5.2 语义上下文、方案身份和实体名称安全

- 前置：B5.1。
- 实施：建立内部 `ConversationTurnContext`，保存累计项目目标、最新指令、目标 Agent、active artifact/plan reference 和约束。
- 入口：旧 persona 只作为兼容入口；正式项目产出仍由策划、美术、程序 Agent 和强类型 Artifact 承担。
- 方案：每个可确认方案必须具有稳定 `agent_plan_id/artifact_ref`；确认不得隐式执行房间内任意 active plan。
- 实体：初始 ScenePlan、方案补充和追加实体统一经过 `EntityNameValidator`；“请给出方案、确认开始、讨论一下”等谓词短语不得成为实体。
- 场景类型：新目标从乐园/混合切换为卧室时必须重算 Scene Contract，不保留无依据的 terrain/transition zone。
- 验证：使用本轮完整原始对话，确保 Disney 卧室目标持续存在、长者只回复和确认自己的方案、零语法片段实体。

### B5.3 上下文记录零模型调用与调用预算

- 前置：B5.2。
- 实施：增加纯记录动作 `runtime.plan_context.record` 和 `runtime.agent_reply_context.record`，只更新 RuntimeState/OperationLog。
- 规则：普通讨论、Agent 回复写回和相同版本重放不得运行场景提取 ToolGraph 或 Provider。
- 提取：只在方案冻结/确认生成时，按 `plan_id + plan_version + content_hash` 幂等执行一次。
- 观测：记录 `message_id/correlation_id/purpose/provider/model/plan_version/dedupe_result`，每条消息输出调用摘要。
- 预算：确定性 GM/查询为 0 次；明确 Agent 讨论最多 1 次用户可见推理；上下文记录为 0 次。

### B5.4 进度、Finalizer、报告和启动路径收口

- 前置：B5.0；可在 B5.2/B5.3 的不重叠模块上准备测试，但生产入口修改必须串行合并。
- 进度：同一计划使用稳定 event ID 原位更新；只有阶段、进度或错误变化才发布，不向聊天追加相同 heartbeat。
- 文本：修复系统名称和用户可见乱码；内部 Runtime/Replay/Provider 大摘要不进入普通聊天。
- 启动：统一 Quasar import 根；同一进程只允许一次 AI service、PoolRegistry 和路径配置初始化。
- Finalizer：用户披露顺序固定为 Engine Snapshot、Readiness、Registry、Consistency、SceneWorldSnapshot、Report、report_ready。
- 报告：最终回复显示实体数、Engine-ready、Game-ready、needs-review、缺失字段、scene version 和 fingerprint。

## 13A. D1-D5：B7 方案权威、真实三职能与媒体链收敛

目标生产链固定为：

```text
用户 <-> GM / CollaborationCoordinator
-> discussion
-> Planning: GameDesignBrief + LevelPlan
-> Program round 1: GameplayLogicPlan + GameplayEntitySlot
-> Art: ArtDirection + SceneCompositionPlan + image_prompts
-> GM 单一正式方案
-> 用户确认 proposal_id + proposal_version + proposal_hash
-> text_to_image
-> image_to_3d
-> AgentRuntime Actor import
-> Readiness / Registry / Snapshot / Report
-> Program round 2: EntityBindingPlan + GameplayManifest
-> ProjectGate -> gameplay.apply_manifest
```

### D1 方案权威与确认事务

- 全链唯一身份为 `proposal_id + proposal_version + proposal_hash`；用户可见文本、`gm_proposal`、Frontend、pending registry 和 Runtime enqueue 必须一致。
- Runtime 外部执行键固定为 `proposal_id@version:hash`；兼容 `artifact_ref=legacy-plan:<proposal_id>` 不得复用旧 Runtime plan。
- 确认同时匹配 ID/version/hash，enqueue 成功后才消费 pending；失败恢复 pending。
- 无语义变化不升版本；有效修订递增版本、更新 Artifact refs/hash 并记录 supersedes。

### D2 真实三职能与专用 ProposalNarrator

- 模型调用顺序固定为 `planning_artifact_reasoning -> program_artifact_reasoning -> art_artifact_reasoning -> collaboration_proposal_narration`。
- 四次调用均为无工具直接模型调用；不得经过 LANChat persona、IntentUnderstanding、旧 planning gate 或 Runtime。
- 每个职能只输出对应强类型 Artifact；schema/hash 失败立即停止，不能创建可确认方案。
- GM 使用专用 ProposalNarrator 汇总一条 `gm_proposal`；输出不得泄露提示词、DTO、Runtime 诊断或执行承诺。

### D3 真实图片资源解析与严格图生模型

- image adapter 必须识别 `llm_content[].part[].content_url=fileid://...`，经注入的 MediaRegistry resolver 等待 ready。
- 图片 `content_hash` 只能来自文件/数据字节或 Provider 权威 hash，禁止对 URL 字符串做伪 hash。
- 图片失败码固定为 `image_tool_call_failed/image_resource_timeout/image_resource_resolve_failed/image_content_hash_missing`。
- 模型和 Actor 导入前都必须验证 `image_to_3d + source_image_ref/hash` 精确匹配；不允许 `text_to_3d` 降级。

### D4 失败计划终态与 Finalizer 幂等

- 终态键固定为 `plan_id + scene_version + snapshot_fingerprint + terminal_status`。
- completed、failed、cancelled 均只 Finalize 一次；已有同终态键 `report_ready` 时禁止再创建 Snapshot/Report/Internal Finalizer graph。
- 终态后清除 active execution 和 retry 状态；讨论、问候、查询和修订不得重新激活计划。
- Finalizer probe 按终态键检查重复与顺序，不使用全局最大 seq 推断。

### D5 进度、问候与用户披露

- 同一 proposal 使用一个稳定 ProgressEvent ID；策划、程序、美术、GM 阶段原位更新并使用不同文案。
- 讨论/问候只读取当前消息、目标 Agent 和累计项目目标，不注入旧 Runtime 报告或 plan ID。
- `gm_proposal` 只有携带 `reply_to + reply_contract=planning_proposal` 才计为 final business reply。
- B7.1 模型预算固定为 discussion `1`、proposal `4`、confirmation `0`、greeting `1`。

完成口径：D1-D5 自动测试通过只能标记 `code_complete`；B7.1/B7.2 必须由新的独立 F5 Session 提供实机证据。

### E5-E9 美术契约与控制面实机收敛

- E5.0：固化 `2026-07-21_02-54-38` 失败证据，状态校准为 `collaboration_art_contract_failed`。
- E5.1：Gameplay semantic role 由 Program 契约权威传递；Art 只生成视觉方向，系统组装最终 SceneCompositionPlan。
- E5.2：Coordinator 模型调用移出状态锁，AttemptReport 实时记录 `in_progress/completed/blocked/not_started`。
- E5.3：协作模型每阶段 90 秒、零自动重试，并记录 output mode、预算、耗时和结果。
- E5.4：正式方案、状态和失败统一由 GM 披露；B7.1 Probe 按四类方案 purpose 对账。
- E5.5：聚焦回归通过后使用全新 Session 重跑 B7.1；READY 前不得进入 B7.2。

### E6 程序契约、硬超时与协作状态收敛

- E6.0：固化 `2026-07-21_04-20-32` 日志和 history；最新探针基线为 `PASS=13 / WARN=1 / FAIL=4`，历史证据不改写。
- E6.1：Program Reasoner 使用以规范化 `semantic_role` 为键的内部 `entity_roles` 对象；系统组装既有 `GameplayLogicPlan.entity_slots`，公共 Artifact 字段不变。
- E6.2：增加应用级模型调用 deadline；Planning/Program/Art 各 90 秒、Narrator 60 秒、完整 Proposal 180 秒。迟到结果不得提交，同房间遗留调用未退出时 fail closed 为 `collaboration_invoker_saturated`。
- E6.3：存在 active/recent AttemptReport 时，通用 GM 状态查询优先返回最多六行的协作阶段状态；显式 Runtime/Engine 查询继续进入 Runtime 只读路径。
- E6.4：首次方案失败保持 `plan_drafting`，协作契约失败使用 `collaboration_blocked`；讨论、协作查询和失败回复不写 AgentRuntime 上下文、模型摘要或内部 ToolGraph。
- E6.5：Probe 将单阶段超过 90 秒、Proposal 超过 180 秒、迟到结果和控制面 Runtime 污染判为 FAIL；Skeleton v6 和既有 hash 保持不变。

E6 自动验证通过后只能标记 `code_complete / pending_f5`。只有全新 Session 输出 `B7_1_CONTROL_READY / WARN=0 / FAIL=0`，才可进入 B7.2。

---

## 14. B6：协作层生产接入与单人 Gate

### B6.1 真实 UserCommand -> DemoRunner 只读接入

- 前置：B5.1、B4.2。
- 实施：Frontend Adapter 或兼容聊天入口把项目请求转换为 `UserCommand`，调用现有 DemoScenarioRunner 和 ProjectGatePreflight。
- Red 边界：只产出 Artifact bundle、Preflight、DemoResult 和 ProgressEvent；零 ActionProposal、PlanPatch、ToolCallGraph 和 Runtime 写入。
- 完成：实际用户入口首次经过 B0-B4 的正式公共接口；旧 RoleAgent 不再创建项目计划。

### B6.2 current-unversioned-v1 Adapter 对账

- 前置：B2.2a。
- 实施：用本轮实机字段 fixture 对账 Engine capability、Snapshot identity、actual AABB、render readiness 和 scene version。
- 完成：当前 SHA 可严格归一化；未知或漂移输入明确 blocked，不使用默认值伪造事实。
- 跨 SHA：继续等待 B2.2b，不把当前 fixture 宣称为通用 Engine 契约。

### B6.3 single_player_demo Gate profile

- 前置：B3、B4；可与 B5 并行实现纯 evaluator，不依赖 B2.2b。
- 实施：增加 `single_player_demo` profile 和动态 `DemoReadinessRequirement`。
- 完成：不硬编码钥匙、门、终点角色；完整 R3 Gate 结果不受影响。
- 验证：全部满足、缺环境、缺 slot、实体 needs_review、fingerprint 冲突、peer 非零和 current fixture blocked。

### B6.4 EntityBindingPlan / GameplayManifest 执行边界

- 前置：B7.2 通过、Single-player Demo Gate Green。
- 实施：生成真实 EntityBindingPlan、GameplayManifest 和 `single_player_local` ActionProposal wire shape。
- 已批准的 PlanPatch 载荷：`patch_type=gameplay_manifest_apply`，并携带 `payload_schema_version/structured_payload/payload_hash/proposal_id`；禁止编码进 `text/items`。
- Runtime 提交：`ActionProposal -> versioned PlanPatch -> business_action ToolCallGraph -> RuntimeGuard -> EngineWriteGate -> gameplay.apply_manifest`。
- 激活条件：代码可以提前完成并聚焦验证，但只有 B7.2 通过、真实 capability manifest 广告 `gameplay.apply_manifest` 与全部所需原语后，生产 Runtime 才注入 provider 并注册该工具；缺能力时零 PlanPatch、零 ToolGraph、零 Engine 写入。
- 幂等：相同 `command_id + payload_hash` 只允许一个 PlanPatch、一个业务图和一次 manifest apply receipt。
- 硬边界：ActionProposal 构造必须执行 `assert_executable()`；Mock、legacy、Red/Yellow Gate、过期 Snapshot 均不能执行。
- 完成：只注册固定单人 Demo 所需白名单原语，不开放任意脚本或 Engine 公共写方法。
- 实机验收入口：`docs/probes/B6.4Gameplay写入回执_F5验收包.md`。

---

## 15. B7：三段实机验收

### B7.1 控制面 F5

固定复现五个业务回合，并增加一个不计入业务方案回合的问候相关性探针：

```text
@小女孩 围绕迪士尼乐园主题讨论一下
@小女孩 按照迪士尼风格的卧室来设计呢
@GM 确认生成
@长者 请你给出一个方案
@长者 确认开始
@小女孩 你好
```

通过标准：六条消息各有一个 route、一个目标 Agent、一次权威执行和一条 final reply；讨论/问候各一次 `agent_visible_reasoning`；每个方案依次且仅调用 `planning_artifact_reasoning -> program_artifact_reasoning -> art_artifact_reasoning -> collaboration_proposal_narration`；确认固定零模型调用。讨论/问候由显式 persona 回复，正式方案、协作状态和失败由 GM 回复；方案与确认携带相同 `proposal_id/version/hash/artifact_refs`。Full R3 Red 时确认只验证引用与 Gate 阻断，不消费 pending proposal，也不创建 Runtime 写入对象。

唯一验收入口：`docs/probes/B7.1控制面F5验收包.md`。使用
`python docs/probes/r3_f5_log_check.py --profile control-plane --history <history> <log>`
自动对账；只有摘要为 `B7_1_CONTROL_READY` 且无 WARN/FAIL 时，B7.1 才可标记 verified。

### B7.2 Scene Runtime 最小 F5

固定输入：

```text
room_box + room_floor + 1 个普通 Actor
```

必须核对：Native scene 不反复 reload；Actor 不重复创建；actor/entity/asset ID 稳定；actual AABB/render observation 可获得；Registry、Snapshot、Report 终态完整；report_ready 是最后的业务终态披露；业务终态后 internal ToolGraph 不无界增长。

媒体链必须由日志逐实体证明：

```text
text_to_image
-> image resource_ref + content_hash
-> image_to_3d
-> model source_image_ref/hash 与图片完全相等
-> engine_actor_import
```

出现 `mock_reference`、`text_to_3d`、缺 hash、hash 不一致或 Runtime-only Actor 均直接失败。自动入口：

```powershell
python docs/probes/r3_f5_log_check.py --profile scene-runtime <本次_corona.log>
```

只有摘要为 `B7_2_SCENE_READY` 且无 WARN/FAIL，才可继续 B6.4 激活和 B7.3。

B7.2 失败时不得注册 B6.4 的生产执行入口。

唯一验收清单：`docs/probes/B7.2最小SceneRuntime_F5验收包.md`。

### B7.3 完整单人 Demo

运行两个独立 Session：

```text
Session A：全新 room/project/plan/runtime state/registry
Session B：再次全新 room/project/plan/runtime state/registry
```

每个 Session 完成：

```text
出生 -> 拾取钥匙 -> 门解锁 -> 到达终点 -> complete_objective
```

共同标准：必要环境和动态 GameplayEntitySlot 实体全部 Game-ready；EntityBindingPlan 只包含真实 entity ID；GameplayManifest 只应用一次；Finalizer、Registry、Snapshot、Report 数量和版本一致；`single_player_demo` Gate Green；`full_r3` Gate 继续如实记录剩余项。

另做一次同 Session、相同 `command_id` 重放，必须零重复 PlanPatch、Actor 和 GameplayManifest。

唯一验收清单：`docs/probes/B7.3单人垂直切片双Session_F5验收包.md`。

---

## 16. 七天时间盒

| 时间 | AI / Runtime | Engine | Frontend | 当日证据 |
|---|---|---|---|---|
| 第1天 | B5.0 证据基线 + B5.1 单消息权威 | 提供当前 Snapshot/manifest 样例 | 确认消息与状态事件语义 | F5 replay、单执行/单回复测试 |
| 第2天 | B5.2 上下文/方案身份/实体校验 | 无新增写入 | 兼容入口只传 UserCommand | 原始对话语义回归 |
| 第3天 | B5.3 零模型上下文记录 + B5.4 进度/终态测试 | 核实 Quasar/Engine 启动入口 | 稳定 progress event 更新语义 | 调用预算、Finalizer 顺序测试 |
| 第4天 | B2.2a + B6.1/B6.3 | 冻结当前未版本化 fixture | 接入只读 DemoRunner/Preflight | Adapter/Gate/入口集成证据 |
| 第5天 | B5 收口门禁 + B7.1 控制面 F5 | 提供候选稳定 SHA | 验证单回复与进度更新 | 控制面 F5 证据包 |
| 第6天 | B7.2 Runtime 最小 F5；通过后 B6.4 | 只修 Snapshot/Readiness 阻断 | 验证终态报告 | 最小实体世界证据包 |
| 第7天 | B7.3 两个独立 Demo Session | 只修白名单原语阻断 | 验证预览和完成态 | 两个独立 Session 证据包 |

日期是时间盒，不是越过依赖和 Gate 的许可。

---

## 17. 分级验证预算

| 改动类型 | 默认验证 | 不需要 |
|---|---|---|
| DTO/schema | syntax + 对应单元测试 | F5、全量 Engine build |
| BlockedResult/Skeleton | 结构化依赖 Validator + Manifest/hash + 端到端骨架 + 依赖方向测试 | F5、Provider、Engine build |
| Track B | 相关模块测试 + 五 Artifact workflow | Runtime 大门禁 |
| Engine Test Double | 生命周期与 Adapter fixture 测试 | 真实 Provider、F5 |
| 消息/回复控制面 | 本轮 history replay + Ledger 并发/重放测试 + final reply 计数 | Engine 渲染、全量 R3 |
| 语义上下文/实体校验 | 原始对话 fixture + plan identity + EntityNameValidator 聚焦测试 | F5、Provider 批量生成 |
| 模型调用预算 | purpose ledger + Provider spy + 零副作用断言 | 真实 DMX 压测 |
| Finalizer/进度披露 | Test Double 终态顺序 + 稳定 event ID/节流测试 | 每次小修 F5 |
| Gate profile | R3 聚焦测试 + 零副作用检查 | 全量 628 项 |
| Frontend Adapter | 映射、幂等和 schema fixture | Engine 渲染验证 |
| Engine Adapter 集成 | 聚焦测试 + 修改目标编译 | 无关模块全编译 |
| 控制面/最小/完整实机 | F5 replay 探针 + R3 日志探针 + 分阶段证据包 | 无关森林、混合、多人场景 |

`verify_ultimate_plan.py` 只在 B5 收口、B7.1 前、B7.3 前或最终提交前运行；同一轮默认最多一次。

---

## 18. 失败处理与阻断规则

- B5.0 无法稳定复现本轮问题：不得修改生产入口，先修证据提取和 fixture。
- B5.1 未通过：暂停语义和执行接入，避免在重复调度上继续叠加逻辑。
- B2.2b 未提供：允许完成当前 SHA 的 B2.2a/B6.3，但跨 SHA 集成保持 blocked。
- B6.1 在 Red 状态尝试写 Runtime：立即停止并回滚该任务自身的接入设计。
- B7.2 前缺少 `gameplay.apply_manifest` 或任一 primitive：B6.4/B7.3 标记 blocked，不回退任意脚本。
- 没有稳定 Engine SHA：最高状态为当前 SHA `integration_ready`，不得宣称跨版本可迁移。
- capability manifest 与实现不一致：由对应底层 owner 修正，AI 层不猜测。
- B7.1 失败：只修消息认领、目标路由、回复仲裁、上下文和进度事件。
- B7.2 失败：只修 Adapter、Engine observation、Snapshot、Readiness 或 Finalizer 断点。
- Frontend 未稳定：使用 Demo Runner 完成后端集成证据，不阻塞后端 `integration_ready`。
- RuntimeGuard 绕过、Snapshot identity 漂移、Mock 可执行或用户改动冲突：立即停止并记录 blocker。
- 同一技术假设连续失败两次：停止继续补丁，重新诊断接口和事实来源。
- 节点需要 F5 后，只允许继续与该真实 Engine 结果无依赖的节点；消费该结果的下游节点保持 blocked。
- 填充 AI 发现公共接口不足时提交 InterfaceChangeRequest，交回架构 AI，不得自行改签名。

---

## 19. 当前明确不做

- 多人实机验收。
- 森林和室内外混合场景完整 F5。
- 任意 Python、Blockly、ScriptBundle 或蓝图执行。
- 战斗、剧情、数值和任务系统 Agent。
- VLM 扩展和高质量美术优化。
- 新 Provider、旧 Workflow 清理和大范围 UI 重构。
- 继续扩建“小女孩/长者/商人”等旧 persona 的项目规划或 Engine 执行能力。
- 用单人 Gate 替代完整 R3 Gate。
- 为投稿单独扩大工程范围。

工程过程应保留 Artifact、Gate、Mock 拒绝和最终 Demo 证据，供后续教学分享使用，但不新增本周阻断任务。

---

## 20. AI 续跑协议

后续 AI 每轮必须执行：

```text
读取本计划与 R3-min推进记录
-> 读取最新 GateReport / integration status
-> 确认当前 B 工作块和任务编号
-> CodeGraph 核实现有实现与依赖
-> 只选择一个最早 ready 的任务
-> 最小实现
-> 按验证预算测试
-> 回写证据、状态和下一任务
```

角色阶段固定为：

```text
黑盒架构阶段：B0-B4 已完成；B6.4 批准结构化 PlanPatch 后已按流程冻结 Skeleton v5
实机收敛架构阶段：B5.0 定义证据、错误预算和生产入口边界
实机收敛填充阶段：按 B5.1 -> B5.2 -> B5.3 -> B5.4 串行收口
并行纯逻辑阶段：B2.2a -> B6.3，不消费尚未验证的 Engine 结论
```

填充 AI 每轮开始必须校验 Skeleton v5 hash、读取最新 F5 issue baseline 和当前任务的 BlockedResult；每轮结束更新生产接入状态，但不得把 B0-B4 的 contract verified 改写为 Engine verified。若修改 Walking Skeleton 公共 DTO/Protocol，仍必须提交 InterfaceChangeRequest；内部控制面 DTO 不自动触发 Skeleton 升版。

确定性选题顺序：

```text
B5.0 未完成 -> B5.0
B5.0 完成 -> B5.1
B5.1 完成 -> B5.2
B5.2 完成 -> B5.3
B5.3 完成 -> B5.4
并行且不修改同一生产入口 -> B2.2a / B6.3
B5.1 完成且 B4.2 verified -> B6.1 只读接入
B5 完成 + B6.1/B6.2/B6.3 integration_ready -> B7.1
B7.1 通过 -> B7.2
B7.2 通过且 single_player_demo Green -> B6.4
B6.4 完成 -> B7.3
```

每轮最终只报告：

```text
完成的任务编号
修改范围
验证证据
当前 integration / Gate 状态
[待 F5/实机验证] 项
Skeleton contract version/hash
当前节点及 BlockedResult
下一个 ready 任务
```

禁止行为：

- 因为无法 F5 就把 Test Double 结果写成 verified。
- 在 `lanchat_agent_worker.py` 或 `agent_runtime/core.py` 继续堆积三职能业务逻辑。
- 修补旧 persona 固定模板来替代策划、美术、程序 Artifact 主链。
- 在 B5.1 前并行修改 Native Queue 与 Agent Trigger 的回复路径。
- 重复实现已有 Artifact、R3GateReport、RuntimeCppBridge 或 Frontend transport Bridge。
- 为保持忙碌新增计划外 Agent、测试矩阵、UI 或 Provider。
- 在没有新证据时修改 Gate 结果或完成声明。
